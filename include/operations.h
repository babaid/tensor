#pragma once
#include "tensor.h"
#include "vector.h"
#include "matrix.h"

template<typename T>
class operations
{
public:
	operations() = default;
	~operations() = 
	static T dot(vector<T>&, vector<T>&);

};